<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
</head>

<body>
  <div style="text-align: center">
    <img id="TextImg">
    <script>
      var assetName = "<%= src %>";
      // var assetUrlParts = window.frameElement.src
      //   .replace('https://wal.lrc.cornell.edu', '..')
      //   .replace(/\/*/, '')
      //   .split('/')
      //   .slice(0, -1);
      // assetUrlParts.push(assetName);
      document.getElementById('TextImg')
        .setAttribute('src', assetName);
    </script>
  </div>
</body>

</html>